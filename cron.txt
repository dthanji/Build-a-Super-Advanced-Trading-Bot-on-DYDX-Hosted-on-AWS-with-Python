# The legacy course cron commands are intentionally retired.
#
# For continuous operation, install systemd/dydx-bot.service instead:
#   sudo cp systemd/dydx-bot.service /etc/systemd/system/
#   sudo systemctl daemon-reload
#   sudo systemctl enable --now dydx-bot
#   sudo journalctl -u dydx-bot -f
#
# Keep the service in paper/dry-run mode while validating the deployment.
